{% extends 'base.html.twig' %}

{% block title %}events{% endblock %}

{% block body %}

<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; min-height: 100vh; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {% if is_granted('ROLE_USER') %}{# {% if user.getSexe()="M" %}M. {% else %}Mme. {% endif %}{{ user.getLogin() }} #}{% endif %}!</h1>

    Choisissez le festival auquel vous voulez participer :
    <div class="box">
        <input type="text" placeholder="Search...">
            <a href="#">
                <img id="saf" src="{{ asset("icons/search.svg") }}" alt="Search icon">
            </a>
    </div>
    {#
        {% for festival in festivals %}
        {{ include('festival/fest.html.twig',{'festival':festival}) }}
        {% endfor %}
    #}
</div>
{% endblock %}
